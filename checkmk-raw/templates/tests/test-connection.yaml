apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "checkmk-raw.fullname" . }}-test-connection"
  labels:
    {{- include "checkmk-raw.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "checkmk-raw.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
